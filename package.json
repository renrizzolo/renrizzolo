{
  "name": "renrizzolo",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll"
  },
  "dependencies": {
    "@stencil/core": "1.0.3",
    "@stencil/router": "one",
    "ip-stencil-route-listener": "0.0.2"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/postcss": "^1.0.1",
    "@types/autoprefixer": "^9.5.0",
    "autoprefixer": "^9.6.0"
  }
}
